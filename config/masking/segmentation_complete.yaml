# @package _global_

masking:
  type: segmentation
  strategy: complete
  pc_ratio: 0.0
  pixel_ratio: 0.0
  ratio:  ${decimal_2_percent:${masking.pixel_ratio}}
  str_ratio: ${convert_str:${masking.ratio}}

